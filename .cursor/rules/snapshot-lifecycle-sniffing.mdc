---
description: 快照 / 页身份 / 挡层生命周期软嗅探——组合信号命中时提醒，非硬门禁
alwaysApply: true
---

# 快照生命周期软嗅探

本文是**软提醒**，不是硬门禁，不强制把一切做成完整状态机文档。

原则缩写见：`docs/AI与人类/规范和原则/快照诚实与失败方向.md`。

## 不做什么

- 不因单信号提醒或升级设计。
- 不自动扩大当前任务范围。
- 普通文案、纯类型整理、无行为变更的注释：**不提醒**。

## 组合风险信号（≥3 类才提醒）

1. **异步 / 迟到：** `snapshot()` Promise、截图回调、延迟后再扫。
2. **对象可能变化：** DOM 重建、Portal 挂载、表格 remount、点位节点卸载、ShadowRoot 替换。
3. **外部可能接管：** Polarise Modal/Popover、Admin chrome、宿主并行改 DOM、另一套自动化同时扫。
4. **高影响副作用：** 误把感知做成 click/eval、截图把整页当操作面、向调用方推全量树。
5. **代际 / 归属：** `pageTitle` 现读 vs 缓存、`createSense` 实例是否仍挂在原 `root`、焦点层与全页两套 id、`resolve` 是否另开名单。

命中 **≥3 类** → 先提醒；用户同意后再做轻量检查。

## 提醒句式（短）

> 这里可能不是普通 API 改动，而是快照/页身份/挡层生命周期风险（已命中：…）。建议先确认当前合同、失败方向和「这一帧菜单是否仍成立」，避免假菜单或把 generic ref 当 playable。要不要先做一轮轻量检查？

## 轻量检查（用户同意后）

1. **当前合同：** 本轮负责 / 不负责什么。  
2. **生命周期：** 全页、焦点层、退化、已换 pagetitle。  
3. **Proof：** 推进前要证明什么（pagetitle 现读、层内 playable 计数、root 仍连接）。  
4. **失败方向：** fail-closed（诚实 `code` / 空列表）还是重试；禁止默默给出可点菜单。

## STOP static expansion

当需求范围明确、当前合同内缺口已关、核心 DTO 有测试时，建议：**停止静态扩张 → playground 对照真实 DOM**。
