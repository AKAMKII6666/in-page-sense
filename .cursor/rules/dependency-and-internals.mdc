---
description: 依赖与包体积——新增依赖与锁文件规则
globs:
  - "package.json"
  - "package-lock.json"
  - "pnpm-lock.yaml"
  - "yarn.lock"
alwaysApply: false
---

# 依赖与内部约定

## 必须执行

- 运行时依赖默认尽量少。允许截图专用依赖 **`modern-screenshot`**（整页诊断图）；新增其它运行时依赖前必须说明：用途、为何不够、体积影响。
- 不得引入 jQuery、大型 UI 库、自动化框架（Playwright 等）作为本库运行时依赖。
- **不得**默认引入 html2canvas；诊断截图走内置 `modern-screenshot` + 可选 `captureScreenshot` 覆盖底图；失败时 `screenshot` / `currentView` 双 `null`。
- 不得引入第三方「DOM→线框 IR」作为运行时依赖。
- 不得在同一任务中顺手升级无关依赖。
- `package.json` 与锁文件必须同步。

## 默认执行

- 绑定行为合同的关键工具链版本宜钉精确版本（避免安静漂移）。
- 使用实验性 / 非标准浏览器 API 时，在代码旁说明原因与降级策略。
