---
description: docs/ 文档执行规范——编码前读什么、变更后写哪、如何固化
globs:
  - docs/**
  - src/**
alwaysApply: false
---

# 文档执行规范

与 `docs-governance.mdc`、`规则制定原则.mdc`、`testing-and-quality.mdc` 衔接。

**冲突裁决：** `codingRole.mdc` > 本规范 > `docs/AI文档/` > `docs/AI与人类/` > 对 `docs/人类文档/` 之外的推断。

入口：[`docs/README.md`](../../docs/README.md)

## 1. 三层职责（执行视角）

| 层 | 放什么 | 何时写 |
|----|--------|--------|
| `docs/人类文档/` | 原始手稿 | **仅人类** |
| `docs/AI文档/` | 已对齐 harness | 对话确认后 **指令固化** |
| `docs/AI与人类/` | 需求拟定、原则、任务、复盘 | 实施过程中可更新 |

## 2. 编码前必读（Agent 必须执行）

| 任务类型 | 必读（按顺序） |
|----------|----------------|
| **任意非 trivial** | `docs/AI与人类/需求拟定/README.md` |
| 模式分流 / 挡层 / pagetitle | 需求拟定 `02`；原则「快照诚实与失败方向」 |
| 扫描 / 树 / 岛 | 需求拟定 `03` |
| 公开 API / DTO | 需求拟定 `04` |
| 目录 / 分期 | 需求拟定 `05` |
| 是否可做控制/BOM/MCP/截图依赖 | 需求拟定 `06` + `codingRole.mdc`（答案通常是否） |

读完后一句话判断：**本次改动是否触及已声明非目标或公开合同**。触及则先澄清。

## 3. 变更后写哪

| 发生了什么 | 写到哪里 |
|------------|----------|
| 修 bug、局部实现，约定未变 | 不写或仅 PR 说明 |
| 行为合同 / API 草案调整 | `AI与人类/需求拟定/`（或决策短文） |
| 原则/失败方向调整 | `AI与人类/规范和原则/` |
| 产品/工程约定已定稿 | **指令固化** → `AI文档/` |

**默认：** 不确定落点时写 `AI与人类/`；不要猜 harness。

## 4. 固化流程

仅当用户意图明确为固化时：

1. 对齐差异与将改路径。  
2. 与 `codingRole.mdc`、需求拟定非目标对照。  
3. 只改点名的 `AI文档/**`。  
4. 在需求拟定文首标注已固化回链。  
5. 报告已固化 / 未固化项。

**禁止：** 未获固化指令时修改 `AI文档/`。

## 5. STOP

P0/P1 清了、核心契约有测 → 建议停止静态扩文档/扩范围，进入 playground 对照 DOM。
