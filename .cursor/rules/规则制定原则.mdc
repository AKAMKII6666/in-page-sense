---
description: in-page-sense 规则制定与冲突裁决的元原则
alwaysApply: true
---

# 规则制定原则

此文件用于解释和裁决 `.cursor/rules/` 中的开发规则，不替代具体领域规则。

## 最高目标

所有规则最终服务于：让维护者更快理解、定位、调试并安全修改代码。

- 优先降低人的认知与调试成本，不追求最短代码。
- 显式表达通常优于隐藏分支、副作用和隐式约定。
- 一致性很重要，但不能反过来降低局部可读性。
- 注释解释意图、原因、约束和异常情况，不只翻译代码。
- 抽象必须减少真实复杂度或形成稳定边界，不为减少行数而抽象。
- 修改应聚焦当前任务；不顺手重构、格式化或补完无关功能。

## Think Before Coding（编码前先想清楚）

约束 Agent **何时停、说什么、问什么**，减少默默假设、藏起困惑、在歧义任务上直接开写。

**与「更短实现」的裁决：** 若更少行数与可调试性、显式分支、职责边界冲突，以本库可维护性与架构规则为准。

### 必须停下来

出现以下任一情况，**先澄清或呈现选项，再写代码**：

- 需求有多种合理解读（例如「支持快照」可能指自主菜单、退化 a11y、或把 screenshot 当点击目标）。
- 协议或语义未写清（`TSenseSnapshot` 判别、`code`、焦点层是否同时收列表和树、generic 是否可点）。
- 改动跨层但用户未指明落点（改 `scan/` vs `tree/` vs `generic/` vs 公开 `types`）。
- 存在明显更简单的替代路径（用户要求新抽象，但现有模块已有同类实现）。
- 可能与 `docs/AI与人类/需求拟定/` 或已固化 `docs/AI文档/` 冲突（职责原子、非目标、快照合同）。
- 触及「假菜单」风险（把 generic ref 写入 playables、无 pagetitle 却出自主菜单、挡层下仍列出底下 id）。
- 成功标准模糊（「优化一下」没有可验证检查项）。

明显 trivial 任务（错别字、单行 import、用户已给出精确文件与行号）可跳过下列准备，但仍不得扩大改动范围。

### 编码前最小准备

非 trivial 任务在动手前完成：

1. **读上下文**：相关模块、调用方；触及需求时读 `docs/AI与人类/需求拟定/`；触及快照失败方向时读 `docs/AI与人类/规范和原则/快照诚实与失败方向.md`。
2. **显式列出假设**（不确定的标注「待确认」）：用户意图一句话；改动落点（scan / tree / generic / 公开 API）；是否触及 DTO 契约测试。
3. **有歧义时呈现选项**，不默默选一个。
4. **有 tradeoff 时必须说出来**：复杂度 vs 可调试性（本库偏后者）；本次范围 vs 顺带修遗留；页内扫描 vs 引入 Playwright/截图库。

### 主动 push back

- 方案把控制、BOM、编排、MCP、扩展桥、宿主 env 闸门、React 打点组件做进本库。
- 为单次使用新建抽象、配置层或「预留扩展」。
- 方案会扩大 diff（顺手重构）；**但**为遵守目录职责聚类而分子目录属于必须。
- 快照 DTO / 模式分流未定就先写实现再「到时候对齐」。
- 为截图引入默认运行时依赖（html2canvas 等）。

表述格式：**「更简单或更安全的做法是 X；若仍坚持 Y，影响是 Z」**。

### 向用户提问

- 一次问**阻塞实现**的关键点；typecheck / 分层常识无需确认。
- 问题要**可回答**：给选项或建议默认。
- 文档与代码能自行核实的，先查再问。

### 多步任务计划

```
1. [步骤] → verify: [typecheck / lint / 具体测试 / playground 检查点]
2. [步骤] → verify: [...]
```

成功标准须具体，不得使用「弄好就行」。

## 规则等级

### 必须执行

职责边界、公开 API 合同、失败方向、质量门禁、目录职责聚类。不得因个人风格偏好绕过。

### 默认执行

函数写法、拆分粒度等。只有在默认写法明显增加认知成本时才允许偏离。

### 推荐实践

局部紧凑写法、显然字段的注释取舍。

**不属于推荐实践（已升为必须）：** 文件头「模块名称/模块说明」、非显然分支的原因注释、公开导出 `interface` 字段级意图 JSDoc、**目录职责聚类（反平铺）**。

## 冲突裁决

1. 失败方向正确性与禁止假菜单（模式 / `code` / 列表 id 诚实）。
2. 职责边界（感知 vs 控制 vs 编排 vs 通道）。
3. 可测试性与 DTO 契约。
4. 人类阅读与调试效率。
5. 局部风格统一。
