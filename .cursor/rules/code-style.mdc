---
description: TypeScript 可读性、注释和调试友好规则
globs:
  - "src/**/*.ts"
alwaysApply: false
---

# 代码风格

本规则强调显式、低歧义和方便断点调试。统一形式服务于可读性，不为形式统一牺牲可读性。

## 默认执行

- 默认使用具名 `function` 声明或 function expression，便于识别调用栈和设置断点。
- 复杂分支使用 `if`、`switch` 或 IIFE 显式展开。
- `if`、`else`、循环和 `catch` 使用花括号并换行。
- 高阶函数中的复杂回调换行并显式 `return`；复杂回调提取为具名函数。

## 允许的简化

- 简单单层三元允许；禁止嵌套三元或在三元中隐藏副作用。
- 简短、无副作用且一眼可读的数组转换等回调允许箭头函数和表达式返回。
- 无副作用的简单提前返回可以保持紧凑，但同一代码块内应保持一致。

## 必须执行

- 禁止在 `&&`、三元、逗号表达式等紧凑语法中隐藏副作用（尤其是写回包字段、截断、模式切换）。
- 禁止无意义缩写和无法从上下文理解的名称。
- 特殊 `switch case`、贯穿逻辑和非显然分支必须注释**原因**（写「为什么」）。
- 注释解释意图、原因、边界与异常情况；禁止仅翻译标识符或复述类型名。
- 导出函数、导出类型和关键约束使用 JSDoc（入参/返回含义，不复述 `string`/`boolean`）。
- **导出 `interface` 字段级 JSDoc（必须）：** 每个公开 `export interface`（尤其 `TSenseSnapshot` 相关、`ICreateSenseOptions`）的每个字段须有 `/** 意图/约束 */`；禁止只给 interface 写总注释而字段全空。
- 新建 `*.ts` 模块须有文件头「模块名称 / 模块说明」注释块。
- 模式分流、焦点层、generic 合并禁令等与需求拟定 `02` / `04` 对齐的行为，若实现有意偏离，必须在代码旁注释原因并同步文档——禁止默默漂移。
