---
description: docs/ 三层治理——人类文档 / AI与人类 / AI文档 的读写边界
globs:
  - docs/**
alwaysApply: false
---

# 文档三层治理

`docs/` 按 **写权限与真源角色** 分三层；与 `codingRole.mdc` 冲突时以代码铁律为准。

## 层级

| 目录 | 人类 | Agent | 内容 |
|------|------|-------|------|
| `docs/人类文档/` | **可写** | **只读，禁止 edit** | 原始方案、手稿 |
| `docs/AI与人类/` | 可写 | 可写 | 进行中：需求拟定、原则、任务、复盘 |
| `docs/AI文档/` | **只读审查，禁止直接改文件** | **可写**（须用户明确要求固化/更新 harness） | 已固化产品/工程约定 |

## Agent 必须执行

- **禁止**编辑 `docs/人类文档/**`（除非用户明确授权「改人类手稿」并说明文件）。
- **禁止**在未获用户明确意图时编辑 `docs/AI文档/**`。允许触发示例：「固化到 AI 文档」「更新 harness」。
- 日常实施、排错记录写入 `docs/AI与人类/**`。
- 文档冲突时真源顺序（当前阶段）：`docs/AI与人类/需求拟定/` →（若已固化）`docs/AI文档/` → `docs/AI与人类/归档/`（若有）。

## 人类协作约定

- 审查 AI 理解：读 `AI文档/`（固化后）或当前 `需求拟定/`；通过指令让 Agent 固化。
- 原始意图变更：改 `人类文档/`，再指令 Agent 更新需求拟定或 AI文档。

入口：[`docs/README.md`](../../docs/README.md) · 执行流程见 [`docs-execution.mdc`](./docs-execution.mdc)
